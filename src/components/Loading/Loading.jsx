import React from 'react';
import './Loading.css';

const Loading = () => (
    <div className="wrap-loading">
        <div className="pokeball" />
        <span>Cargando...</span>
    </div>
);

export default Loading;
